export type Testimonial = {
  quote: string;
  name: string;
  title: string;
  avatarUrl: string;
};


export type Feature = {
    icon : React.ReactElement,
    title : string,
    description : string,
    color: string
}
